[workspace]
members = ["control_flow","guessing_game", "hello_cargo", "ownership", "roket-api"]

resolver = "2"


## 2. 構造を確認

# ディレクトリ構造はこうなっているはずです：

# hello_rust/
# ├── Cargo.toml          # ← ワークスペース設定（新規作成）
# ├── guessing_game/
# │   ├── Cargo.toml      # ← 既存（そのまま）
# │   └── src/
# ├── hello_cargo/
# │   ├── Cargo.toml      # ← 既存（そのまま）
# │   └── src/
# └── hello_rust/
#     ├── Cargo.toml      # ← 既存（そのまま）
#     └── src/
